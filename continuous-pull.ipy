#!/usr/bin/ipython3

# Note: set `git config pull.rebase false` so that there will be no warning
# every time

import time

while True:
    !echo    
    !date    
    !git pull
    time.sleep(60)

# Note: when Amarillo runs with `uvicorn amarillo.main:app --reload` and not with
# `python main.py` then there is nothing else to do. The reload happens
# automatically.